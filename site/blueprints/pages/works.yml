title: Works

create:
  redirect: true
  status: unlisted
  
# options:
#   delete: false
#   duplicate: false
#   changeStatus: false
#   move: false
#   sort: false

image:
  icon: page
  query: false

columns:
  main:
    width: 3/4
    sections:
      items:
        label: "{{ page.title }}"
        type: pages
        layout: cards
        size: large
        # layout: table
        # info: "{{ page.slug }}"
        # columns:
        #   title: true
        #   info: false
        #   year: true
        #   client: true
        #   location: true
        #   category: true
        #   stat:
        #     label: Status
        template:
          - work
        search: false
        sortable: true
        image:
          ratio: 4/3
          cover: true
          query: page.cover.toFile()
  sidebar:
    width: 1/4
    sections: