name: Image
icon: image
wysiwyg: true
preview: images
fields:
  images:
    label: Image
    type: files
    template: image
    uploads: image
    query: page.files.filterBy("type", "in", ["image", "video"])
    multiple: false
    image:
      ratio: 4/3
      cover: true
  caption:
    label: false
    type: writer
    placeholder: Caption...
    inline: true
    marks:
      - bold
      - italic
      - underline
      - strike
      - link
      - clear
    nodes: false

  
